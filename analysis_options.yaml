# This file configures the analyzer and linter for the Flutter project.
#
# For details on configuring this file, see:
# https://dart.dev/guides/language/analysis-options

include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "lib/l10n/app_localizations*.dart"
    # Flutter generated
    - ".dart_tool/**"
    - "build/**"
    - "build/web/**"
    # Swift Package Manager & CocoaPods
    - "ios/Pods/**"
    - "build/ios/**"
    # Native platforms
    - "android/**"
    - "ios/**"
    - "web/**"
    - "windows/**"
    - "macos/**"
    - "linux/**"

  errors:
    unused_import: error
    unused_local_variable: warning
    dead_code: warning

linter:
  rules:
    # Error Prevention & Async Safety
    - cancel_subscriptions
    - avoid_void_async
    - no_adjacent_strings_in_list
    - unnecessary_statements
    - throw_in_finally
    - unnecessary_await_in_return
    - prefer_void_to_null
    - noop_primitive_operations

    # Code Quality & Immutability (Const Rebuild Optimizations)
    - prefer_const_constructors
    - prefer_const_literals_to_create_immutables
    - prefer_const_declarations
    - prefer_final_in_for_each
    - always_declare_return_types
    - depend_on_referenced_packages
    - use_string_buffers
    - sort_unnamed_constructors_first
    - unnecessary_parenthesis
    - unnecessary_breaks
    - combinators_ordering
    - dangling_library_doc_comments
    - library_annotations
    - tighten_type_of_initializing_formals

    # Flutter Performance & Widget Tree Polish
    - sized_box_shrink_expand
    - use_colored_box
    - use_decorated_box