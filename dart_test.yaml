concurrency: 4
timeout: 60s

tags:
  golden:
    description: "Tests that generate golden files."
  screenshot:
    description: "Integration tests that capture UI screenshots on Android emulator."
    timeout: 300s
